# Quick Start Guide 
Clone the repo and then run the below commands to get started.
```
sudo npm install -g cordova
sudo npm install -g shelljs
sudo npm install -g ios-sim
export NODE_PATH=/usr/local/lib/node_modules
export CLIENT=topteachers
node bin/setup_client.js
```
